# Production email config (loaded by firebase-functions params on deploy).
# Using Resend test sender — only delivers to the account owner address below.
# Once a real domain is verified in Resend, update MAIL_FROM and clear MAIL_REDIRECT.
MAIL_FROM=GearTrack <onboarding@resend.dev>
MAIL_REDIRECT=digital_development@mercedes-benz.com
APP_URL=https://mbtraininguk.github.io/GearTrack/
