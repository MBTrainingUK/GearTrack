# Production email config (loaded by firebase-functions params on deploy).
# MAIL_FROM must use a domain verified in Resend before going live.
# MAIL_REDIRECT is deliberately unset in production.
MAIL_FROM=GearTrack <notifications@YOUR-VERIFIED-DOMAIN>
APP_URL=https://mbtraininguk.github.io/GearTrack/
