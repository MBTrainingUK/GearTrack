# Staging email config (loaded by firebase-functions params on deploy).
# All staging email is redirected to MAIL_REDIRECT — real users are never emailed.
MAIL_FROM=GearTrack <onboarding@resend.dev>
# Note: until a domain is verified in Resend, the test sender can only
# deliver to the Resend account owner's address below.
MAIL_REDIRECT=digital_development@mercedes-benz.com
APP_URL=https://geartrack-staging.web.app/
